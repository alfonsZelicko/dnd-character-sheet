import React from 'react';
import { Grid2 as Grid } from '@mui/material';
import { StyledPaper } from '../shared';

export const MoneyAndLoot = () => {
  return <Grid container size={12} component={StyledPaper}></Grid>;
};
